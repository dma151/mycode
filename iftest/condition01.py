#!/usr/bin/env python3

# create the string hostname
hostname = "MTG"

# test logic with if
if hostname == "MTG":
    # action to take if statement was true
    print("The hostname was found to be MTG")
